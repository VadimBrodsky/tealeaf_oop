class Fruit
  def initialize(name)
    name = name
  end
end

class Pizza
  def initialize(name)
    @name = name
  end
end

# The Class Piazza has an instance variable, because of the @ sign that denotes the @name

# Instance variables can be called by
# object.instance_variables
